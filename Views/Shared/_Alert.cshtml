@if (TempData["Message"] != null)
{
    <div class="alert @TempData["Class"] alert-dismissible fade show" role="alert" id="Myalert"
    style="display: block; position: absolute; left: 50%; transform: translateX(-50%); width: max-content;">
    @TempData["Message"]
</div>
}